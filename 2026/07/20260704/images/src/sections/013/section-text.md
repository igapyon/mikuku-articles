# Java 版の batch 変換例

- Java 版では複数ファイルをまとめて変換
- directory を指定して変換
- recursive で下位フォルダも走査

## 図解

```text
複数 .docx
  -> Java CLI
  -> 複数 .md

input directory
  -> output directory
```

## 短いラベル

- multiple files
- input directory
- output directory
- recursive scan

