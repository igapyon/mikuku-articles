# 対応 runtime

- Node.js CLI と Java CLI がある
- どちらも v1.2.1 を確認対象
- 単一ファイル変換はほぼ同じ引数
- Java 版には batch / directory 向けの追加引数

## 図解

```text
Node.js CLI
  -> single .docx

Java CLI
  -> single .docx
  -> batch / directory
```

## 短いラベル

- release tag
- artifact
- runtime difference
- Java extensions

