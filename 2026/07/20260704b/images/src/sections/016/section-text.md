# Agent Skill 経由で使う

- `igapyon-miku-ms-office` 経由では出力形式を明示

```text
convert ./docs/spec.md to ./workplace/spec.docx
```

- Markdown 入力だけでは Office 形式が曖昧
- `.docx` / `.xlsx` / `.pptx` を言葉にする
- 変換の向きを迷子にしない

