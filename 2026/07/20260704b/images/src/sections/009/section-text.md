# 共通オプション

- Node.js 版と Java 版で主なオプションは共通

| option | 役割 |
| --- | --- |
| `--out` | DOCX 出力先 |
| `--summary` | summary を stdout |
| `--summary-out` | summary を file |
| `--verbose` | stderr 診断 |
| `--version` | version |
| `--help` | help |

- 通常利用は `<input.md>` + `--out`

