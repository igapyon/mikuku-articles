# はじめに

- Markdown を Word `.docx` へ
- Markdown は AI agent と開発者に扱いやすい
- でも共有・提出・レビューでは Word が必要
- `miku-md2docx` は出口の道具

```text
Markdownで書く
  ↓
Wordで渡す
```

- authoring system ではない
- Markdown の本文構造を Word に包み直す

