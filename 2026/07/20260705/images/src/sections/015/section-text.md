# Agent Skill 経由で使う

- `igapyon-miku-ms-office` 経由
- 入力と出力を明示
- backend を Node.js / Java で指定可能
- 主出力は Markdown
- summary / JSON / assets / debug は必要時に追加依頼

```text
convert sample.pptx
  -> sample.md
backend: Node.js or Java
```

- Agent Skill でも CLI と同じ考え方で扱う

