# 対応 runtime

- Node.js CLI
- Node.js runtime bundle
- Node.js source archive
- Java CLI
- Java source archive

```text
v0.5.1
  Node.js 版
  Java 版
  --help option set は同じ
```

- 実行形は runtime ごとに違う
- CLI 契約はそろえて説明できる

