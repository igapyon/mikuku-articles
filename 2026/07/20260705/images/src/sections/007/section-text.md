# 基本コマンド

- Node.js 版
- Java 版
- `--out` で Markdown ファイルへ保存
- 省略時は stdout
- `--no-notes` で speaker notes を省略
- `--assets-dir` で画像 asset を sidecar へ

```text
input.pptx
  --out output.md
  --no-notes
  --assets-dir output.assets
```

- 後続作業ではファイル保存のほうが扱いやすい

