# `--help` 出力の確認

- CLI 契約を raw text として確認
- 入力は local `.pptx` 1 つ
- 主出力は Markdown
- summary / summary-out / summary-json-out
- verbose は stderr
- help / version は metadata command

```text
USAGE
CONTRACT
OPTIONS
OUTPUTS
EXAMPLES
EXIT CODES
```

- 人間には長くても、AI agent には機械可読な契約文になる

