# auto-waiting がうれしい

- Web テストは待ち時間で不安定になりやすい
- 固定秒数ではなく、要素が操作できる状態になるまで待つ
- 非同期処理、画面切り替え、API 結果描画に向く

```text
固定秒数で待つ
  → タイミングに弱い

状態を見て待つ
  → テストが少し落ち着く
```

キーワード:

- auto-waiting
- 状態を見て待つ
- 現代的 Web アプリ
